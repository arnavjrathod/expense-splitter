Create the application as defined by the prompt. Steps:
1. create an implementation plan
2. create the application
3. create unit tests
4. run unit tests. if fail, fix the bugs
5. run the application, smoke test it. if errors found, fix them.
6. end ONLY when the smoke test is successful

# developement
always make it portable. use venvs and node_modules in the same repo, to make it portable.
for python, use the uv package manager.
